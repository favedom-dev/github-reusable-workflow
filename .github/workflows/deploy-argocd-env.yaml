name: Deploy Environment

on:  # yamllint disable-line rule:truthy
  workflow_call:
    inputs:
      NAME:
        required: true
        type: string

      VERSION:
        required: true
        type: string

      MONOREPO_APP:
        default: ''
        required: false
        type: string

      ENV_REPO_NAME:
        default: 'favedom-dev/argocd-deployments'
        required: false
        type: string

      ENV_REPO_BRANCH:
        default: 'master'
        required: false
        type: string

      ENV_REPO_PATH:
        default: 'env-repo'
        required: false
        type: string

      ENV_REPO_SUBDIR_BASE:
        default: 'dev/fanfuzenil'
        required: false
        type: string

      ENV_REPO_SUBDIR:
        required: false
        type: string

      OVERRIDE_SUBDIR_NAME:
        required: false
        type: string

      DO_PR_MERGE:
        default: 'true'
        required: false
        type: string

      VERSION_SCRIPT_BASE_URL:
        default: 'https://raw.githubusercontent.com/favedom-dev/github-reusable-workflow/'
        required: false
        type: string

      VERSION_SCRIPT_BRANCH:
        default: 'v2'
        required: false
        type: string

      VERSION_SCRIPT_DIR:
        default: '.github/scripts'
        required: false
        type: string

      VERSION_SCRIPT_NAME:
        default: 'update-values-tag.sh'
        required: false
        type: string

      YAML_FILE:
        default: 'values.yaml'
        required: false
        type: string

      GH_USER_NAME:
        default: 'PowerVZ GitHub Bot'
        required: false
        type: string

      GH_USER_EMAIL:
        # default: 'github@powervz.com'
        default: ''
        required: false
        type: string

      GH_RUNS_ON:
        default: 'ubuntu-latest'
        required: false
        type: string

    secrets:
      GH_TOKEN:
        required: true

jobs:

  deploy-environment:
    timeout-minutes: 5
    runs-on: ${{ inputs.GH_RUNS_ON }}
    steps:

      # - name: "🕸️ DEBUG: inputs"
      #   run: |
      #     echo "NAME                    : ${{ inputs.NAME }}"
      #     echo "VERSION                 : ${{ inputs.VERSION }}"
      #     echo "MONOREPO_APP            : ${{ inputs.MONOREPO_APP }}"
      #     echo "ENV_REPO_NAME           : ${{ inputs.ENV_REPO_NAME }}"
      #     echo "ENV_REPO_BRANCH         : ${{ inputs.ENV_REPO_BRANCH }}"
      #     echo "ENV_REPO_PATH           : ${{ inputs.ENV_REPO_PATH }}"
      #     echo "ENV_REPO_SUBDIR_BASE    : ${{ inputs.ENV_REPO_SUBDIR_BASE }}"
      #     echo "ENV_REPO_SUBDIR         : ${{ inputs.ENV_REPO_SUBDIR }}"
      #     echo "OVERRIDE_SUBDIR_NAME    : ${{ inputs.OVERRIDE_SUBDIR_NAME }}"
      #     echo "VERSION_SCRIPT_BASE_URL : ${{ inputs.VERSION_SCRIPT_BASE_URL }}"
      #     echo "VERSION_SCRIPT_BRANCH   : ${{ inputs.VERSION_SCRIPT_BRANCH }}"
      #     echo "VERSION_SCRIPT_DIR      : ${{ inputs.VERSION_SCRIPT_DIR }}"
      #     echo "VERSION_SCRIPT_NAME     : ${{ inputs.VERSION_SCRIPT_NAME }}"
      #     echo "YAML_FILE               : ${{ inputs.YAML_FILE }}"

      - name: "⬇️ Get reusable files"
        uses: favedom-dev/github-reusable-workflow/.github/actions/get-reusable-files@v2
        id: get-reusable-files
        with:
          FILE_GIT_BRANCH: ${{ inputs.VERSION_SCRIPT_BRANCH }}
          FILE_DOWNLOAD_BASE_URL: ${{ inputs.VERSION_SCRIPT_BASE_URL }}
          FILES_TO_DOWNLOAD: |
            [
              { "subdirectory": "${{ inputs.VERSION_SCRIPT_DIR }}", "filename": "${{ inputs.VERSION_SCRIPT_NAME }}" }
            ]

      - name: "🔩 Git config"
        uses: favedom-dev/github-reusable-workflow/.github/actions/setup-git-config@v2
        with:
          GH_USER_NAME: ${{ inputs.GH_USER_NAME }}
          GH_USER_EMAIL: ${{ inputs.GH_USER_EMAIL }}

      - name: "☁️ Checkout environment repository: ${{ inputs.ENV_REPO_NAME }} branch: ${{ inputs.ENV_REPO_BRANCH }}"
        if: github.ref == 'refs/heads/master'
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.ENV_REPO_NAME }}
          token: ${{ secrets.GH_TOKEN }}
          ref: ${{ inputs.ENV_REPO_BRANCH }}
          path: ${{ inputs.ENV_REPO_PATH }}

      - name: "📝 Set ENV_REPO_SUBDIR"
        id: set-env-repo-subdir
        env:
          OVERRIDE_SUBDIR_NAME: ${{ inputs.OVERRIDE_SUBDIR_NAME }}
          SUBDIR_NAME: ${{ inputs.NAME }}
        run: |
          echo "SUBDIR_NAME         : ${SUBDIR_NAME}"
          echo "OVERRIDE_SUBDIR_NAME: ${OVERRIDE_SUBDIR_NAME}"
          if [ -n "${ENV_REPO_SUBDIR}" ]; then
            ENV_REPO_SUBDIR=${{ inputs.ENV_REPO_SUBDIR }}
            echo "if ENV_REPO_SUBDIR: ${ENV_REPO_SUBDIR}"
          else
            if [ -n "${OVERRIDE_SUBDIR_NAME}" ]; then
              SUBDIR_NAME=${{ inputs.OVERRIDE_SUBDIR_NAME }}
            fi
            ENV_REPO_SUBDIR=${{ inputs.ENV_REPO_SUBDIR_BASE }}/${SUBDIR_NAME}
            echo "else ENV_REPO_SUBDIR: ${ENV_REPO_SUBDIR}"
          fi
          echo "ENV_REPO_SUBDIR: ${ENV_REPO_SUBDIR}"
          echo "ENV_REPO_SUBDIR=${ENV_REPO_SUBDIR}" >> $GITHUB_OUTPUT

      - name: "👁️‍🗨️ PRE-update: ./${{ inputs.YAML_FILE }}"
        if: github.ref == 'refs/heads/master'
        working-directory: ${{ inputs.ENV_REPO_PATH }}/${{ steps.set-env-repo-subdir.outputs.ENV_REPO_SUBDIR }}
        run: |
          pwd
          cat ./${{ inputs.YAML_FILE }}

      - name: "🔄 Update ${{ inputs.NAME }} version ${{ inputs.VERSION }} in environment repo: ${{ inputs.YAML_FILE }}"
        if: github.ref == 'refs/heads/master'
        working-directory: ${{ inputs.ENV_REPO_PATH }}/${{ steps.set-env-repo-subdir.outputs.ENV_REPO_SUBDIR }}
        env:
          VERSION_SCRIPT_DIR_URL: ${{ inputs.VERSION_SCRIPT_BASE_URL }}/${{ inputs.VERSION_SCRIPT_BRANCH }}/${{ inputs.VERSION_SCRIPT_DIR }}
          VERSION_SCRIPT_NAME: ${{ inputs.VERSION_SCRIPT_NAME }}
          NAME: ${{ inputs.NAME }}
          VERSION: ${{ inputs.VERSION }}
          YAML_FILE: ${{ inputs.YAML_FILE }}
          # REGISTRY_PROTOCOL: ${{ inputs.REGISTRY_PROTOCOL }}
          # REGISTRY: ${{ inputs.REGISTRY }}
          # REPOSITORY: ${{ inputs.REPOSITORY }}
          # ROOT_ELEMENT: ${{ inputs.ROOT_ELEMENT }}
          # SUB_ELEMENT: ${{ inputs.SUB_ELEMENT }}
          # BASE_ELEMENT: ${{ inputs.BASE_ELEMENT }}
        run: |
          echo "Update values.yaml ${NAME}.image.tag"
          pwd
          ls -la${{ github.workspace }}/..
          echo "--------------------"
          ls -la
          echo "===================="
          if [ -f "${{ github.workspace }}/../${{ inputs.VERSION_SCRIPT_NAME }}" ]; then
            cp ${{ github.workspace }}/../${{ inputs.VERSION_SCRIPT_NAME }} ./${{ inputs.VERSION_SCRIPT_NAME }}
            echo "Copied ${{ inputs.VERSION_SCRIPT_NAME }}"
          else
            echo "File does not exist: ${{ github.workspace }}/../${{ inputs.VERSION_SCRIPT_NAME }}"
          fi
          # wget -O ./${VERSION_SCRIPT_NAME} ${VERSION_SCRIPT_DIR_URL}/${VERSION_SCRIPT_NAME}
          # chmod 777 ./${VERSION_SCRIPT_NAME}
          ./${{ inputs.VERSION_SCRIPT_NAME }} . "${NAME}" "${VERSION}"
          rm ./${{ inputs.VERSION_SCRIPT_NAME }}
          echo ""
          echo "🛈 NOTE: MAY NEED TO EXPAND THIS!!!"

      - name: "👁️ Updated: ./${{ inputs.YAML_FILE }}"
        if: github.ref == 'refs/heads/master'
        working-directory: ${{ inputs.ENV_REPO_PATH }}/${{ steps.set-env-repo-subdir.outputs.ENV_REPO_SUBDIR }}
        run: |
          pwd
          cat ./${{ inputs.YAML_FILE }}

      - name: "🡢 Create environment PR in repo: ${{ inputs.ENV_REPO_NAME }}"
        uses: favedom-dev/github-reusable-workflow/.github/actions/create-env-pr@v2
        if: github.ref == 'refs/heads/master'
        with:
          env-repo-name: ${{ inputs.ENV_REPO_NAME }}
          env-repo-path: ${{ inputs.ENV_REPO_PATH }}
          name: ${{ inputs.NAME }}
          version: ${{ inputs.VERSION }}
          do-pr-merge: ${{ inputs.DO_PR_MERGE }}
          is-helm-chart: 'false'
          monorepo-app: ${{ inputs.MONOREPO_APP }}
          gh-token: ${{ secrets.GH_TOKEN }}
      # - name: "🡢 Create environment PR in repo: ${{ inputs.ENV_REPO_NAME }}"
      #   if: github.ref == 'refs/heads/master'
      #   working-directory: ${{ inputs.ENV_REPO_PATH }}
      #   env:
      #     NAME: ${{ inputs.NAME }}
      #     VERSION: ${{ inputs.VERSION }}
      #     BRANCH_NAME: gha_${{ inputs.NAME }}_${{ inputs.VERSION }}
      #     GH_TOKEN: ${{ secrets.GH_TOKEN }}
      #     MONOREPO_APP: ${{ inputs.MONOREPO_APP }}
      #     DO_PR_MERGE: ${{ inputs.DO_PR_MERGE }}
      #   run: |
      #     # set -e  # Exit immediately if any command fails
      #     MAX_RETRIES=5
      #     SLEEP_TIME=10

      #     echo "Checking if branch '${BRANCH_NAME}' already exists..."
      #     if git ls-remote --exit-code --heads origin "${BRANCH_NAME}"; then
      #       echo "Branch '${BRANCH_NAME}' exists. Checking it out..."
      #       git checkout "${BRANCH_NAME}"

      #       echo "Rebasing '${BRANCH_NAME}' onto '${MAIN_BRANCH}'..."
      #       git fetch origin "${MAIN_BRANCH}"
      #       git rebase origin/"${MAIN_BRANCH}" || {
      #         echo "Rebase conflict detected. Aborting rebase..."
      #         git rebase --abort
      #         echo "Skipping rebase. Continuing with existing branch..."
      #       }
      #     else
      #       echo "Creating new branch '${BRANCH_NAME}'..."
      #       git checkout -b "${BRANCH_NAME}"
      #     fi

      #     echo "Staging and committing changes..."
      #     git add .
      #     if git diff --staged --quiet; then
      #       echo "No changes to commit. Exiting..."
      #       exit 0
      #     fi

      #     echo "git commit -a -m \"${NAME} ${VERSION}\""
      #     git commit -a -m "${NAME} ${VERSION}"

      #     echo "Force pushing branch to remote..."
      #     git push --force-with-lease origin "${BRANCH_NAME}"

      #     FULL_VERSION=v${VERSION}
      #     if [ ${#MONOREPO_APP} -gt 0 ]; then
      #       FULL_VERSION=${MONOREPO_APP}/${FULL_VERSION}
      #     fi

      #     echo "Fetching release notes for ${FULL_VERSION}..."
      #     gh release view ${FULL_VERSION} -R ${{ github.repository }} --json author,tagName,url,body --template \
      #     'Version: [{{.tagName}}]({{.url}})

      #     {{.body}}
      #     ' >> ./GH_RELEASE_NOTES.txt

      #     GH_BODY=$(cat ./GH_RELEASE_NOTES.txt)
      #     # echo "GH_BODY : ${GH_BODY}"

      #     echo "Creating pull request..."
      #     for ((i=1; i<=MAX_RETRIES; i++)); do
      #       PR_URL=$(gh pr create --label "approved" --title "chore: bump image.tag ${NAME} to ${VERSION}" --body "${GH_BODY}" 2>/dev/null)
      #       if [ $? -eq 0 ]; then
      #       # if gh pr create --label "approved" --title "chore: bump image.tag ${NAME} to ${VERSION}" --body "${GH_BODY}"; then
      #         echo "Pull request created successfully."
      #         echo "PR URL: ${PR_URL}"
      #         echo "# Pull Request" >> $GITHUB_STEP_SUMMARY
      #         echo "🡢 Image tag updated in \`${{ steps.set-env-repo-subdir.outputs.ENV_REPO_SUBDIR }}\` : [${NAME} ${VERSION}](${PR_URL})" >> $GITHUB_STEP_SUMMARY
      #         break
      #       fi
      #       echo "PR creation failed. Retrying in ${SLEEP_TIME} seconds... (Attempt ${i}/${MAX_RETRIES})"
      #       sleep $(( SLEEP_TIME * i ))  # Exponential backoff
      #     done

      #     if [ "${DO_PR_MERGE}" = "true" ]; then
      #       echo "Auto Squash & Merge PR..."
      #       for ((i=1; i<=MAX_RETRIES; i++)); do
      #         if gh pr merge "${BRANCH_NAME}" --auto --squash; then
      #           echo "PR merged successfully."
      #           break
      #         fi
      #         echo "PR merge failed. Retrying in ${SLEEP_TIME} seconds... (Attempt ${i}/${MAX_RETRIES})"
      #         sleep $(( SLEEP_TIME * i ))  # Exponential backoff
      #       done
      #     fi
