name: "PR Merged → Slack Notification"

on:
  workflow_call:
    inputs:

      channel-id:
        default: "#test-bot"
        description: "Slack channel ID or name (e.g. deploys or C1234567890)"
        required: false # true
        type: string

      marker:
        description: "Text to identify the changelog comment"
        default: "<!-- Image Generated:"
        type: string

      include-changed-files:
        description: "Include list of changed YAML files in message"
        default: false
        type: boolean

      CLUSTER_NAME:
        default: 'core'
        required: false
        type: string

      CLUSTER_PROJECT_ID:
        default: 'core-services-370815'
        required: false
        type: string

      LOCATION:
        default: 'us-central1'
        required: false
        type: string

      # TODO
      SECRETS_TO_FETCH:
        description: "Secrets to fetch from Google Secret Manager"
        required: false
        type: string
        # default: |
        #   SLACK_BOT_TOKEN:projects/1054075584205/secrets/cicd_prod_SLACK_BOT_TOKEN

      DEFAULT_SECRETS_TO_FETCH:
        description: "Default secrets to fetch from Google Secret Manager"
        required: false
        type: string
        default: |
          GH_TOKEN:projects/1054075584205/secrets/core_GITHUB_FAVEDOM_DEV_PASSWORD,
          SLACK_BOT_TOKEN:projects/1054075584205/secrets/cicd_SLACK_DEVOPS_TOKEN

      FILE_DOWNLOAD_BASE_URL:
        default: 'https://raw.githubusercontent.com/favedom-dev/github-reusable-workflow'
        required: false
        type: string

      FILE_GIT_BRANCH:
        default: 'v2' # 'v2.1'
        required: false
        type: string
        description: "Used typically for testing new non-merged helm changes"

    secrets:

      WIF_PROVIDER:
        description: "Workload Identity Federation Provider"
        required: true

      WIF_SERVICE_ACCOUNT:
        description: "Workload Identity Federation Service Account"
        required: true

jobs:
  check-yaml-changes:
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'
    outputs:
      has_yaml_changes: ${{ steps.check.outputs.has_yaml_changes }}
      changed_files: ${{ steps.check.outputs.changed_files }}
      UNIQUE_PAIRS: ${{ steps.check.outputs.UNIQUE_PAIRS }}
      CHANGED_LIST: ${{ steps.check.outputs.CHANGED_LIST }}
      MATRIX_JSON: ${{ steps.check.outputs.MATRIX_JSON }}
    steps:

      - name: "☁️ Checkout with full history"
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: "🔐 Get Secrets"
        uses: favedom-dev/github-reusable-workflow/.github/actions/get-google-secrets@v2.1
        id: get-secrets
        with:
          WIF_PROVIDER: ${{ secrets.WIF_PROVIDER }}
          WIF_SERVICE_ACCOUNT: ${{ secrets.WIF_SERVICE_ACCOUNT }}
          SECRETS_TO_FETCH: ${{ inputs.SECRETS_TO_FETCH }}
          DEFAULT_SECRETS_TO_FETCH: ${{ inputs.DEFAULT_SECRETS_TO_FETCH }}

      - name: "🕵️ Detect YAML changes"
        id: check
        env:
          GITHUB_TOKEN: ${{ env.GH_TOKEN }}
        run: |
          set -euo pipefail

          BASE="${{ github.event.pull_request.base.sha }}"
          HEAD="${{ github.event.pull_request.head.sha }}"

          echo "Comparing $BASE..$HEAD"

          DATA=$(curl -s \
            -H "Authorization: token $GITHUB_TOKEN" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/${{ github.repository }}/compare/$BASE...$HEAD")

          # Extract structured YAML changes: env  tenant  full_path
          PAIRS=$(echo "$DATA" | jq -r '
            .files[]
            | select(.filename | test("\\.(yaml|yml)$"; "i"))
            | .filename as $f
            | ($f | split("/")) as $p
            | select($p | length >= 3)
            | "\($p[0])\t\($p[1])\t\($f)"
          ')

          if [ -z "$PAIRS" ]; then
            echo "has_yaml_changes=false" >> $GITHUB_OUTPUT
            echo "UNIQUE_PAIRS=" >> $GITHUB_OUTPUT
            echo "CHANGED_LIST=" >> $GITHUB_OUTPUT
            echo "MATRIX_JSON=[]" >> $GITHUB_OUTPUT
            echo "No YAML files changed under <env>/<tenant>/ structure"
            exit 0
          fi

          echo "has_yaml_changes=true" >> $GITHUB_OUTPUT

          # Unique env/tenant pairs (preserves relationship)
          # UNIQUE_PAIRS=$(echo "$PAIRS" | cut -f1,2 | sort -u)
          UNIQUE_PAIRS=$(echo "$PAIRS" | awk '{print $1 " " $2}' | sort -u)

          # Build clean changed files list with proper bullet
          CHANGED_LIST=$(echo "$PAIRS" | cut -f3 | sed 's/^/  • /')

          # Build valid JSON matrix
          MATRIX_JSON=$(echo "$UNIQUE_PAIRS" | jq -R 'split(" ") | {env: .[0], tenant: .[1]}' | jq -s .)

          # === Set outputs safely ===
          {
            echo "UNIQUE_PAIRS<<EOF"
            echo "$UNIQUE_PAIRS"
            echo "EOF"

            echo "CHANGED_LIST<<EOF"
            echo "$CHANGED_LIST"
            echo "EOF"

            # This is the correct way to output JSON in GITHUB_OUTPUT
            echo "MATRIX_JSON<<EOF"
            echo "$MATRIX_JSON"
            echo "EOF"
          } >> $GITHUB_OUTPUT

          # Human-readable summary
          echo "YAML changes detected in:"
          echo "$UNIQUE_PAIRS" | awk '{print "  • " $1 "/" $2}'
          echo ""
          echo "UNIQUE_PAIRS:"
          echo "$UNIQUE_PAIRS"
          echo ""
          echo "Changed files:"
          echo "$CHANGED_LIST"
          echo ""
          echo "Pairs:"
          echo "$PAIRS"
          echo ""
          echo "MATRIX_JSON:"
          echo "$MATRIX_JSON"

          echo "Environment:" >> $GITHUB_STEP_SUMMARY
          echo "$UNIQUE_PAIRS" | awk '{printf "  • %s/%s\n", $1, $2}' >> $GITHUB_STEP_SUMMARY
          echo "Changed files:" >> $GITHUB_STEP_SUMMARY
          echo "$CHANGED_LIST" >> $GITHUB_STEP_SUMMARY

  notify-and-track:
    needs: check-yaml-changes
    if: needs.check-yaml-changes.outputs.has_yaml_changes == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'
    outputs:
      ts: ${{ steps.slack.outputs.ts }}
      thread_ts: ${{ steps.slack.outputs.thread_ts }}
    steps:

      - name: "☁️ Checkout"
        uses: actions/checkout@v4

      - name: "⬇️ Get reusable files"
        uses: favedom-dev/github-reusable-workflow/.github/actions/get-reusable-files@v2.1
        id: get-reusable-files
        with:
          FILE_GIT_BRANCH: ${{ inputs.FILE_GIT_BRANCH }}
          FILE_DOWNLOAD_BASE_URL: ${{ inputs.FILE_DOWNLOAD_BASE_URL }}
          FILES_TO_DOWNLOAD: |
            [
              { "subdirectory": ".github/scripts", "filename": "mrkdwn-slack.sh" }
            ]

      - name: "🔐 Get Secrets"
        uses: favedom-dev/github-reusable-workflow/.github/actions/get-google-secrets@v2.1
        id: get-secrets
        with:
          WIF_PROVIDER: ${{ secrets.WIF_PROVIDER }}
          WIF_SERVICE_ACCOUNT: ${{ secrets.WIF_SERVICE_ACCOUNT }}
          SECRETS_TO_FETCH: ${{ inputs.SECRETS_TO_FETCH }}
          DEFAULT_SECRETS_TO_FETCH: ${{ inputs.DEFAULT_SECRETS_TO_FETCH }}

      - name: "🔓 Get GKE Credentials cluster: ${{ inputs.CLUSTER_NAME }}"
        id: 'get-credentials'
        uses: 'google-github-actions/get-gke-credentials@v2'
        with:
          cluster_name: ${{ inputs.CLUSTER_NAME }}
          location: ${{ inputs.LOCATION }}
          project_id: ${{ inputs.CLUSTER_PROJECT_ID }}

      - name: "🏷️ Set up variables"
        id: setup-variables
        run: |
          JSON='${{ needs.check-yaml-changes.outputs.MATRIX_JSON }}'
          TENANT=$(echo "$JSON" | jq -r '.[0].tenant')
          if [ "$TENANT" = "tmp-fanfuze" ] || [ "$TENANT" = "fanfuzenil" ]; then
            TENANT="nilx"
          fi
          echo "Tenant : ${TENANT}"
          echo "TENANT=${TENANT}" >> $GITHUB_ENV
          echo "TENANT=${TENANT}" >> $GITHUB_OUTPUT

      # ------------------------------------------------------------------
      # Find the changelog comment
      # ------------------------------------------------------------------
      - name: "🔎 Find Marker in PR Body"
        id: comment
        env:
          GITHUB_TOKEN: ${{ env.GH_TOKEN }}
        run: |
          # get the PR body directly in JSON format
          PR_BODY=$(gh pr view ${{ github.event.pull_request.number }} \
            --json body \
            --jq '.body')

          BODY=$(echo "$PR_BODY" | awk '/${{ inputs.marker }}/{found=1; next} found')
          if [ -z "$BODY" ]; then
            BODY=$(echo "$PR_BODY" | awk '/Version: /{found=1; next} found')
          fi

          echo "BODY : ${BODY}"

          # Check if BODY is empty
          if [ -z "$BODY" ]; then
            # Simple case: single-line output works fine without a delimiter
            echo "comment=${{ needs.check-yaml-changes.outputs.CHANGED_LIST }}" >> $GITHUB_OUTPUT
            exit 0
          fi

          # Convert Markdown to Slack mrkdwn
          # The -e flag ensures proper newline interpretation
          CLEAN_MRKDWN=$(echo -e "${BODY}" | ../mrkdwn-slack.sh)
          echo "CLEAN_MRKDWN : ${CLEAN_MRKDWN}"

          # Escape the Slack Mrkdwn string for safe JSON/YAML embedding.
          #    - Escape backslashes: s/\\/\\\\/g
          #    - Escape double quotes: s/"/\\"/g
          #    - Convert actual newlines to escaped newlines: s/\n/\\n/g
          CLEAN=$(echo "$CLEAN_MRKDWN" | sed -E ':a;N;$!ba;s/\n/\\n/g' | sed -E 's/\"/\\\"/g')
          echo "CLEAN : ${CLEAN}"

          # Use the EOT delimiter for multi-line output to GITHUB_OUTPUT
          # This correctly handles newlines and special characters in the converted text.
          echo "comment<<EOT" >> $GITHUB_OUTPUT
          echo "$CLEAN" >> $GITHUB_OUTPUT
          echo "EOT" >> $GITHUB_OUTPUT

      # ------------------------------------------------------------------
      # Post to Slack and capture ts/thread_ts
      # ------------------------------------------------------------------
      - name: "📝 Post PR message to Slack"
        id: slack
        uses: slackapi/slack-github-action@v2
        with:
          method: chat.postMessage
          token: ${{ env.SLACK_BOT_TOKEN }}
          payload: |
            {
              "channel": "${{ inputs.channel-id }}",
              "text": "🚀 Deployment Update Initiated: PR #${{ github.event.pull_request.number }}",
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "🚀 Deployment Update Initiated: PR #${{ github.event.pull_request.number }}"
                  }
                },
                {
                  "type": "divider"
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*🛠️ Details*"
                  },
                  "accessory": {
                    "type": "image",
                    "image_url": "https://storage.googleapis.com/public-assets-8eb56dad/tenants/${{ steps.setup-variables.outputs.TENANT }}/assets/images/logos/favicon.ico",
                    "alt_text": "tenant favicon"
                        },
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "*Environment:*\n${{ needs.check-yaml-changes.outputs.UNIQUE_PAIRS }}"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*GitHub PR Link:*\n<${{ github.event.pull_request.html_url }}|#${{ github.event.pull_request.number }}>"
                    }
                  ]
                },
                {
                  "type": "divider"
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*📰 Release Changelog*"
                  }
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "${{ steps.comment.outputs.comment }}"
                  }
                }
              ]
            }

      # - name: "DEBUG"
      #   run: |
      #     # https://github.com/slackapi/slack-github-action/blob/26d9f0ada3710dfa07fea2aa146a59e80d978652/action.yml#L52
      #     echo "steps.slack.outputs.ok : ${{ steps.slack.outputs.ok }}"
      #     echo "steps.slack.outputs.response : ${{ steps.slack.outputs.response }}"
      #     echo "steps.slack.outputs.time : ${{ steps.slack.outputs.time }}"
      #     echo "steps.slack.outputs.channel_id : ${{ steps.slack.outputs.channel_id }}"
      #     echo "steps.slack.outputs.ts : ${{ steps.slack.outputs.ts }}"
      #     echo "steps.slack.outputs.thread_ts : ${{ steps.slack.outputs.thread_ts }}"

      # ------------------------------------------------------------------
      # Write PR → Revision + thread_ts mapping to Kubernetes
      # ------------------------------------------------------------------
      - name: "☸️ Create/Update pr-to-revision ConfigMap"
        run: |
          COMMIT=${{ github.sha }}
          cat <<EOF | kubectl apply -f -
          apiVersion: v1
          kind: ConfigMap
          metadata:
            name: pr-to-revision-$COMMIT
            namespace: argocd
            labels:
              app.kubernetes.io/part-of: pr-tracking
          data:
            pr: "${{ github.event.pull_request.number }}"
            url: "${{ github.event.pull_request.html_url }}"
            title: "${{ github.event.pull_request.title }}"
            author: "${{ github.event.pull_request.user.login }}"
            slack_thread_ts: "${{ steps.slack.outputs.ts }}"
          EOF

      # Expose thread_ts for potential downstream jobs
      - name: "🩻 Expose ts/thread_ts"
        run: |
          echo "ts        : ${{ steps.slack.outputs.ts }}"
          echo "thread_ts : ${{ steps.slack.outputs.thread_ts }}"
          echo "ts=${{ steps.slack.outputs.ts }}" >> $GITHUB_OUTPUT
          echo "thread_ts=${{ steps.slack.outputs.thread_ts }}" >> $GITHUB_OUTPUT
